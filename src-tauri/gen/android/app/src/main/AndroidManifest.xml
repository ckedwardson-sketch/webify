<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lock screen list: lets the app set the lock screen wallpaper, and
         re-apply the current frame after a reboot (alarms don't survive
         one). See LockScreenController.kt / LockScreenReceiver.kt. -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Lets the in-app WebView's getUserMedia() (task completion photos,
         QuickPhotoWidget) actually prompt for camera access — without this,
         Android silently denies the permission and the WebView never shows
         a system prompt at all. required="false" so the app still installs
         on a device/emulator with no camera. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.webify"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:allowBackup="false">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Redraws the lock screen list while the app is closed: the
             next-frame alarm, plus reboot / app update / clock changes.
             exported="true" so the system's broadcasts reach it; anything
             that does reach it can only trigger a harmless redraw. -->
        <receiver
            android:name=".LockScreenReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="com.ckedw.webify.LOCKSCREEN_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings tile: remaining checklist count + open to a
             first-level page. User adds it via Edit on the QS panel.
             See ChecklistTileService.kt. -->
        <service
            android:name=".ChecklistTileService"
            android:exported="true"
            android:label="Checklist"
            android:icon="@drawable/ic_qs_checklist"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
